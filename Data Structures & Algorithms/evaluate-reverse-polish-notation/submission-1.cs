public class Solution {
    public int EvalRPN(string[] tokens) {
        Stack<int> stack = new();

        for (int i = 0; tokens.length; i++){
            switch(tokens[i]){
                case "+":
                    break;
                    case "+":
                    break;
                    case "+":
                    break;
            }
        }
    }
}
